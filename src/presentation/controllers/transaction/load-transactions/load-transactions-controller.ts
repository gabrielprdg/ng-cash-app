import { Controller } from '../presentation/protocols'

export class LoadTransactions implements Controller {
  private readonly loadTransactions: LoadTransactions

  constructor (loadTransactions: LoadTransactions) {
    this.loadTransactions = loadTransactions
  }

  async
}
